// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  masterFirebaseConfig: {
  apiKey: "AIzaSyBCTkvNgEFNsAwM3D7Sujc5IWjI11iis_U",
    authDomain: "register-d1780.firebaseapp.com",
    databaseURL: "https://register-d1780.firebaseio.com",
    projectId: "register-d1780",
    storageBucket: "register-d1780.appspot.com",
    messagingSenderId: "743884932250"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
